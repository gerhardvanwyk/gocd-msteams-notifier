pipelines = [{
  name = "defaultRule"
  group = "ci"
  stage = "build"
  # you can provide multiple values by separating them with | (pipe) symbol - failed|broken
  statuses = "[failed, broken]"        # accepted values - failed / broken / fixed / passed / all
}]
