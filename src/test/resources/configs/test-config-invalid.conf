gocd.msteams {
  login = "foo
  password = "foo-bar"
  goCdClient-host = "https://go-instance:8153/"
  webhookUrl = "http://slack.com/"

  pipelines = [{
    name = ".*"
    stage = ".*"
    state = "failed"
    channel = "#foo"
  }]
}
